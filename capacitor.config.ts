import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.indian.nutrition.tracker',
  appName: 'Indian Nutrition Tracker',
  webDir: 'dist'
};

export default config;
